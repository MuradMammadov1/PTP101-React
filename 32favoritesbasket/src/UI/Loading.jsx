import React from 'react';

const Loading = () => (
    <div style={{color: 'white', textAlign: 'center', marginTop: '50px', fontSize: '24px'}}>
        LOADING...
    </div>
);

export default Loading;